  $('.clike').click(function() {
      var clicks = $(this).data('clicks');
      if (clicks) {
         $(".camo").empty())
      } else {
        $(".camo").show();
      }
      $(this).data("clicks", !clicks);
    })