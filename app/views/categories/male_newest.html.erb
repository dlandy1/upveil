<%= link_to "Look by Hottest",  category_male_path(@category), class: 'btn btn-xs btn-primary hay mobile-button' %>
<%= render partial: 'maleheader' %>

<div class="show-box" id="products">
        <div class="page">
        <% @unscope.each do |product| %>
           <%= render product %>
          <% end %>
          </div>
      </div>


     <%= paginate @unscope %>