<%= render partial: 'header' %>


    <div class="show-box" id="products">
        <div class="page">
        <% @unscope.each do |product| %>
           <%= render product %>
          <% end %>
          </div>
      </div>


     <%= paginate @unscope %>
   </div>
  </div>