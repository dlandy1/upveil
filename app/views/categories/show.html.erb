<%= link_to "Look by Newest",  category_newest_path(@category), class: 'btn btn-xs btn-primary hay mobile-button' %>
<%= render partial: 'header' %>




    <div class="show-box newer" id="products"> 
      <div class="page">
      <% @products.all.each do |product| %>
       <%= render product %>
       <% end %>
      </div>
    </div>

 

  <%= paginate @products %>
    </div>