
<h1 class="h3 text-center"><b><%=@category.title %>'s</b><br><p class="h6">Newest and Best Products</p></h1>
 <div class="col-md-4">
      <%= link_to "New Product", new_category_product_path(@category), class: 'btn btn-success pull-right' %>
  </div>
<div class="row">
  <div class="col-md-8">
    <% @products.each do |product| %>
      <%= render @products %>
      <% end %>
  </div>
</div>