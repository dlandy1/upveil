<%= content_tag :div, id: "comment-#{subcomment.id}", class: 'teen' do %>
  <%= link_to '#', class: 'pull-left commentprof lieft' do %>
    <%= image_tag(subcomment.user.identity.smallimage) %>
  <% end %>
  <div class='media-body'>
    <div class="loft">
    <small class="cament">
      <%= subcomment.user.name %> commented <%= time_ago_in_words(subcomment.created_at) %> ago
      <% if current_user %>
      <div class="trosh">
          <% if current_user == subcomment.user || current_user.id == 1 || current_user.id == 2 || current_user.id == 3 || current_user.id == 4  %>
        <%= link_to  edit_category_product_comment_path(subcomment.product.category, subcomment.product, subcomment) ,class: 'flant' do %>
        <%= fa_icon "pencil" %>
       <% end %> 
       <%= link_to [subcomment.product.category, subcomment.product, subcomment] , method: :delete, data: { confirm: 'Are you sure you want to delete this comment?'}, remote: true  do %>
        <%= fa_icon "trash-o" %>
       <% end %> 
      <% end %> 
      </div>
      <% end %> 
    </small>
    </div>
    <p class="dint"><%= subcomment.body %></p>
  </div>
<% end %>