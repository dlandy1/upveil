<% if activity.trackable.purchase %>
clicked buy here on
  <% if activity.trackable %>
    <b><%= activity.trackable.title %></b>
  <% else %>
    which has since been removed
  <% end %>
<% else %>
  upvoted
  <% if activity.trackable %>
    <b><%= activity.trackable.title %></b>
  <% else %>
    which has since been removed
  <% end %>
<% end %>